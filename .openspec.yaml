# OpenSpec 配置 — 食堂物资管理系统
# 详见: .claude/skills/openspec-*/SKILL.md

schema: spec-driven

paths:
  specs: openspec/specs
  changes: openspec/changes
  decisions: openspec/decisions
  archive: openspec/changes/archive

context: |
  食堂出入库管理系统 — Electron + sql.js 桌面应用，管理食堂物资的入库/出库、采购单、询价和台账。
  技术栈: Electron v42, sql.js (SQLite WASM), 原生 JS (无框架), Chart.js, ExcelJS, xlsx。
  详细架构见 openspec/specs/core/spec.md。
