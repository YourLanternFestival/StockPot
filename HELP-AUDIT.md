# 帮助页 & 新手教程审查报告

> 目标用户：40+ 岁食堂经理，非技术背景  
> 审查日期：2026-06-18

---

## 一、现有内容

### 帮助页（index.html `#page-help`，约 520 行）

11 个章节：产品管理 / 入库 / 出库 / 库存 / 台账 / 采购 / 询价 / 数据导入导出 / 历史采购 / 设置 / 常见问题(9个FAQ) + 快捷键表格 + 联系支持

顶部有搜索框 + 目录网格，"回到目录"悬浮按钮，以及"启动新手教程"按钮。

### 新手教程（tour.js，11 步）

| 步骤 | 目标 |
|------|------|
| 1 | 欢迎页 |
| 2 | 侧栏导航 |
| 3 | 仪表盘 |
| 4 | 产品管理（可点击跳转） |
| 5 | 入库登记（可点击跳转） |
| 6 | 出库登记（可点击跳转） |
| 7 | 库存查询与预警（可点击跳转） |
| 8 | 采购单管理 |
| 9 | 历史采购（可点击跳转） |
| 10 | 设置中心 |
| 11 | 快捷键提示 |

首次使用自动弹出（500ms 延迟），可跳过，完成后保存 `tutorial_completed = '1'`。

---

## 二、问题清单

### 高优先级

1. **零截图**：帮助页纯文字，没有任何界面截图。40+ 用户通常是视觉型学习者，标注关键按钮位置的主界面截图至少需要 3-4 张（仪表盘、入库表、出库表、库存预警）。

2. **键盘快捷键轰炸**：Ctrl+D、Ctrl+Enter、回车、方向键、Tab、Escape 在至少 4 个章节中反复出现。"回车导航模式"（跳到下一行同列 vs 下一行首格）对非技术用户来说概念过载。建议统一收到一个"快捷键"章节，其他各处只说"按回车跳到下一行"。

3. **术语"TSV 格式"**：采购单粘贴部分出现了"多列粘贴（TSV 格式）"。对食堂经理来说毫无意义，应改为"表格数据"或"多列数据"。

### 中优先级

4. **缺少"快速入门"章节**：帮助页假设用户知道从哪开始。需要一个明确的"新人上手指南"：产品 → 入库 → 出库 → 库存 → 采购，5 步走完核心循环。

5. **教程 11 步太多**：首次接触信息过载。建议缩到 5 步核心循环（产品→入库→出库→库存→采购），完整版作为可选。

6. **教程没有实操环节**：只是"指给你看"但不让用户动手。应该在步骤中加入"现在试试点击'+ 新增产品'"这样的交互引导。

7. **术语"鉴证表"**：询价部分使用。如果食堂经理日常不用这个词，需要换掉。

### 低优先级

8. **"联系支持"太单薄**：只说"联系系统管理员"，没有具体联系方式或报错机制。

9. **CSS 数据关键词泄露**：帮助页 HTML 的 `data-keywords` 里出现了 `lock EBUSY` 这种内部错误码，不应该让用户看到。

10. **教程遮罩可能错位**：基于 `getBoundingClientRect` 的固定定位，如果目标元素在视口外可能偏移。

---

## 三、建议改进方案

### 方案 A：轻量修补（2-4 小时）
- 删掉各处重复的快捷键描述，收到一个"快捷键速查"章节
- "TSV 格式"→"多列数据"
- 加 3-4 张截图到关键章节
- 在开头加一个"新人上手指南"（5 步纯文字流程）

### 方案 B：重写帮助页（1-2 天）
- 按工作流程重组织：① 新人上手 → ② 日常入库 → ③ 日常出库 → ④ 查库存/预警 → ⑤ 做采购单 → ⑥ 月底台账 → ⑦ 设置与维护
- 每个章节配上标注截图
- 语言降到小学五年级阅读水平（短句、主动语态、避免任何术语）
- 教程重做为 5 步交互式引导

### 方案 C：做视频（额外）
- 用 DEMO-SCRIPT.md 录 3-5 分钟操作演示
- 嵌入帮助页或放在安装目录

---

## 四、下次会话 TODO

1. 用户确认改哪种方案（A / B / C）
2. 如需截图，用户提供界面截图或我们标注 mockup
3. 确认"鉴证表"是否需要换词
4. 确认是否需要修改教程的自动触发逻辑
