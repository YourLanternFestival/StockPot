# 询价管理 - 实物图保存 施工图纸

## 目标

为询价管理页面的商品提供实物图片支持，解决品名歧义问题（如佛豆/蚕豆/甜豆/豌豆）。

## 核心设计

### 图片定位规则

复用采购单逻辑：**品名 + 规格** 唯一定位。

```
文件名格式：{品名}_{规格}.{ext}
示例：佛豆_散装.jpg、老豆腐_箱.png
无规格时：佛豆_.jpg
```

### 图片存储

- 照片文件夹路径在设置页可配置（新增一个设置项 `photo_folder`）
- 默认值建议：空（用户自行设置，指向内网共享文件夹或本地目录）
- EXE 分发版各机器指向同一内网地址即可共享图片

### 支持格式

jpg / jpeg / png / bmp / webp（覆盖微信图片、手机截图、常见照片格式）

---

## 需要改动的文件

### 1. `renderer/index.html`

**设置页新增一项：**
```html
<div class="form-group">
  <label>照片文件夹路径</label>
  <input type="text" class="form-control" id="setting-photo-folder" placeholder="如：\\server\photos 或 C:\photos">
</div>
```

放置位置：设置页现有的卡片中，建议新建一个"📁 文件路径"卡片，或放在"询价管理"卡片内。

### 2. `renderer/styles.css`

新增图片相关样式：
- `.inquiry-img-thumb` — 缩略图（32x32 或 40x40，object-fit: cover，圆角，cursor: pointer）
- `.img-viewer-overlay` — 全屏查看遮罩层
- `.img-viewer-content` — 大图居中显示
- `.img-empty` — 无图时的占位图标（📷 灰色）

### 3. `renderer/app.js`

#### 3a. 设置相关

- `SETTING_KEYS` 数组新增 `'photo_folder'`
- `SETTING_DEFAULTS` 新增 `photo_folder: ''`
- `loadAppSettings` 新增读取 `photo_folder` 到 `APP_SETTINGS.photo_folder`
- `initSettingsPage` / `saveSettings` 对应新增处理

#### 3b. 询价表渲染 — `renderInquiryTable()`

当前每行渲染逻辑（约 line 2585）需要新增图片列：

```
表头新增：<th style="width:60px;">图片</th>
每行新增：<td class="img-cell">{缩略图或占位符}</td>
```

缩略图渲染逻辑：
```
const imgKey = `${item.name}_${item.spec || ''}`;
const imgPath = 查找 photoFolder 中匹配 imgKey.* 的文件
若有 → <img class="inquiry-img-thumb" src="file://..." onclick="viewImage(...)">
若无 → <span class="img-empty">📷</span>
```

**注意**：`file://` 协议在 Electron 中需要在 `main.js` 的 `webPreferences` 中确保可访问本地文件，
或者通过自定义 IPC 读取图片为 base64/dataURL 返回给 renderer。

#### 3c. 图片查找 — 新增函数

```javascript
async function findItemImage(name, spec) {
  // 通过 IPC 调 main 进程，在 photoFolder 中查找匹配文件
  // 返回图片路径或 null
}
```

main.js 中新增 IPC handler：
```javascript
ipcMain.handle('image:find', (e, name, spec, folder) => {
  // 遍历 folder，匹配文件名
  // 支持多种扩展名
  // 返回完整路径或 null
});
```

preload.js 中新增：
```javascript
findImage: (name, spec, folder) => ipcRenderer.invoke('image:find', { name, spec, folder }),
```

#### 3d. 图片查看 — 新增函数

```javascript
function viewImage(imageSrc, itemName) {
  // 创建全屏遮罩层，居中显示大图
  // 点击遮罩或按 ESC 关闭
}
```

#### 3e. 添加图片 — 新增函数

```javascript
async function addInquiryImage(name, spec) {
  // 1. 打开文件选择对话框
  // 2. 用户选择图片
  // 3. 复制到 photoFolder，重命名为 {name}_{spec}.{ext}
  // 4. 刷新该行缩略图
  // 5. 如已存在同名图片，提示覆盖
}
```

main.js 中新增 IPC handler：
```javascript
ipcMain.handle('image:add', (e, name, spec, sourcePath, folder) => {
  // 复制 sourcePath 到 folder/{name}_{spec}.{ext}
  // 如已存在则覆盖
  // 返回目标路径
});
```

preload.js 中新增：
```javascript
addImage: (name, spec, sourcePath, folder) => ipcRenderer.invoke('image:add', { name, spec, sourcePath, folder }),
```

#### 3f. 询价表每行的操作列改造

当前询价表每行无操作列。新增一列"图片"：
- 有图：显示缩略图，点击放大；hover 显示"更换"按钮
- 无图：显示📷占位，点击触发添加图片

### 4. `main.js`

新增两个 IPC handler（见 3c、3e）。

文件操作使用 Node.js `fs` 模块：
- `fs.readdirSync` 遍历文件夹
- `fs.copyFileSync` 复制图片
- `path.extname` 获取扩展名
- `path.join` 拼接路径

### 5. `preload.js`

新增两个 API 方法（见 3c、3e）。

---

## 边界情况处理

| 场景 | 处理方式 |
|------|----------|
| 照片文件夹未设置 | 图片列显示占位符，点击提示"请先在设置中配置照片文件夹路径" |
| 照片文件夹不存在 | 同上，或自动创建（需确认） |
| 同品名不同规格 | 文件名含规格，不会冲突 |
| 同品名同规格多图 | 只保留最后一张（覆盖），或改为 `{品名}_{规格}_{序号}.ext`（复杂，暂不做） |
| 品名含特殊字符 | 文件名中 `/\:*?"<>|` 替换为 `_` |
| 图片文件过大 | 添加时不做压缩，直接复制；缩略图用 CSS 控制显示大小 |
| 内网文件夹不可达 | 优雅降级，显示占位符，不报错 |

---

## 实施顺序

1. **设置页**：新增 `photo_folder` 设置项 + 读写逻辑
2. **IPC 层**：main.js + preload.js 新增 `image:find` 和 `image:add`
3. **询价表改造**：表头加图片列，每行渲染缩略图/占位符
4. **图片查看器**：全屏放大弹窗
5. **添加图片**：文件选择 → 重命名 → 存入文件夹 → 刷新显示
6. **边界处理**：路径未设置、文件夹不存在、特殊字符等

---

## 注意事项

- Electron 中 renderer 进程不能直接访问 `fs`，所有文件操作必须走 IPC 到 main 进程
- `file://` 协议在某些 Electron 版本中可能被拦截，推荐通过 IPC 读取为 base64 后用 `data:image/...;base64,...` 显示
- 缩略图建议懒加载，避免图片过多时卡顿
