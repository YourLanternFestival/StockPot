# 操作演示脚本

目标时长：2-3 分钟。不需要解说词，画面+字幕就够了。

---

## 开场（5 秒）

**画面**：应用启动，总览仪表盘

**字幕**：`食堂出入库管理系统 — Electron 桌面应用`

---

## 1. 入库登记（20 秒）

**操作**：
1. 点击侧栏"入库"
2. 在品名列输入"青"→ 自动补全弹出 → 选择"青菜"
3. 规格、单位自动填入
4. 输入数量"50"，选日期
5. 回车 → 自动跳到下一行
6. 继续输入第二个品名

**字幕**：`品名自动补全，回车自动跳行`

---

## 2. 出库登记 + Ctrl+D（20 秒）

**操作**：
1. 点击侧栏"出库"
2. 输入品名"大米"，选择后自动显示库存
3. 输入数量"20"，选领取人"厨房"
4. 跳到下一行，品名也输入"大米"
5. 按 Ctrl+D → 数量、日期、领取人自动向下填充

**字幕**：`Ctrl+D 向下填充，重复录入不再繁琐`

---

## 3. 采购单 — 粘贴功能（30 秒）

**操作**：
1. 点击侧栏"采购单"
2. 从 Excel 复制一列品名（5-6 行）
3. 粘贴到采购单品名列 → 自动向下填充，序号自动重排
4. 从 Excel 复制多列数据（品名+数量）
5. 粘贴到采购单 → 多列同时填充

**字幕**：`支持单列和多列粘贴，超出行数自动追加行`

---

## 4. 采购单 — 食堂模式切换（15 秒）

**操作**：
1. 打开设置 → 业务 → 食堂模式
2. 切换到"小所食堂"→ 保存
3. 回到采购单 → 显示卫星食堂标签页（食堂F1、食堂F2等）
4. 切换到矩阵模式 → 品名×所名网格

**字幕**：`三种食堂模式，分组/矩阵两种输入样式`

---

## 5. 采购单导出 — 文件占用重试（15 秒）

**操作**：
1. 在采购单页面点击"导出"
2. 选择一个已被 Excel 打开的文件
3. 系统自动以 (2) 后缀另存
4. toast 提示"文件被占用，已另存为 xxx(2).xlsx"

**字幕**：`文件被占用时自动加后缀重试，无需手动关闭 Excel`

---

## 6. 历史采购 — 批量导出（20 秒）

**操作**：
1. 点击侧栏"历史采购"
2. 点击"批量导出"
3. 弹窗勾选 3 个日期
4. 点击导出 → 生成一个 Excel，每天的厨房/联华各一张 sheet

**字幕**：`批量导出多天历史记录，厨房和联华分 Sheet`

---

## 7. 库存查询 + 过期预警（15 秒）

**操作**：
1. 点击侧栏"库存查询"
2. 搜索一个产品 → 显示库存详情和出入库流水
3. 点击"过期预警"标签 → 显示临期/过期物品列表
4. 红色/橙色/黄色标记不同预警等级

**字幕**：`实时库存查询，过期预警可配置天数`

---

## 8. 设置中心（15 秒）

**操作**：
1. 点击侧栏"设置"
2. 展示 4 个标签页：通用 → 数据 → 业务 → 外观
3. 切换到外观 → 修改主色调 → 预览色板实时变化
4. 切换到 Biophilic 主题 → 界面风格变化

**字幕**：`分页标签导航，30+ 配置项，双主题切换`

---

## 收尾（5 秒）

**画面**：回到总览仪表盘

**字幕**：`~8400 行 JS | sql.js + ExcelJS | Electron 42`

---

## 录制提示

- 用 OBS 或 Windows 自带的 Xbox Game Bar 录屏
- 分辨率 1920×1080，不需要录鼠标高亮
- 每个片段单独录，后期拼接（出错了只重录那一段）
- 字幕用剪映或 PR 加，白色字体，底部居中
- 不需要配音，纯画面+字幕更干净
- 背景音乐可选，不要有歌词的
